import formatDate from './formatDate';
import { titleVariant, staggerVariant, fadeIn, slideIn } from './motion';

export {
    titleVariant,
    staggerVariant,
    fadeIn,
    slideIn,
    formatDate,
}
